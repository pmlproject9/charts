apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hascheduller.fullname" . }}-test-connection"
  labels:
{{ include "hascheduller.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "hascheduller.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
