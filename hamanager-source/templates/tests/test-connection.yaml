apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hamanager-source.fullname" . }}-test-connection"
  labels:
{{ include "hamanager-source.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "hamanager-source.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
