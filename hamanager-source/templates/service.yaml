apiVersion: v1
kind: Service
metadata:
  name: {{ include "hamanager-source.fullname" . }}
  labels:
{{ include "hamanager-source.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "hamanager-source.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
